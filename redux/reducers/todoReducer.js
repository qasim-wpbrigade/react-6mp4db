import { ADD_TODO } from '../constants';

const initState = {
  todoList: [],
  todoCompleteList: [],
};

const TodoReducer = (state = initState, action) => {
  return state;
};

export default TodoReducer;
